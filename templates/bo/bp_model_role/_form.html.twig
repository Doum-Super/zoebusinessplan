{{ form_start(form,  { 'attr': {'class': 'form'} }) }}
    <div class="card-body">
        <div class="form-group row">
            <div class="col-lg-6">
                {{ form_label(form.bpModel) }}
                {{ form_widget(form.bpModel) }}
                {{ form_errors(form.bpModel) }}
            </div>
             <div class="col-lg-6">
                {{ form_label(form.role) }}
                {{ form_widget(form.role) }}
                {{ form_errors(form.role) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-lg-12">
                {{ form_label(form.variables) }}
                {{ form_widget(form.variables) }}
            </div>
        </div>
    </div>
    <div class="card-footer text-right">
        <a href="{{ path('bo_bp_model_role_list') }}" class="btn btn-secondary">Retour à la liste</a>
        <button type="submit" class="btn btn-success ml-2">Enregistrer</button>
    </div>
{{ form_end(form) }}
